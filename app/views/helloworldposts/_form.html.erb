<%= form_with(model: helloworldpost, local: true) do |form| %>
  <% if helloworldpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(helloworldpost.errors.count, "error") %> prohibited this helloworldpost from being saved:</h2>

      <ul>
      <% helloworldpost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
